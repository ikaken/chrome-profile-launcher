# Chromeプロファイルランチャ仕様書（WPFネイティブ版・最終）

---

## 1. 概要

本仕様は、Google Chromeの複数プロファイルを効率的に管理・切替するためのWindowsネイティブランチャアプリの設計を定義する。

実装は C#（.NET）および WPF を使用する。

---

## 2. 目的

- ワンクリックでプロファイルを起動・切替
- 起動済みプロファイルの即時フォーカス
- プロファイル情報の自動取得と管理
- 軽量・高速・配布容易なアプリの実現

---

## 3. 対象環境

| 項目 | 内容 |
|------|------|
| OS | Windows 10 / 11 |
| 言語 | C# |
| フレームワーク | .NET |
| UI | WPF |
| 配布形式 | 単一EXE |

---

## 4. 機能要件（詳細）

### 4.1 プロファイル一覧取得

#### 入力
```
%LOCALAPPDATA%\\Google\\Chrome\\User Data
```

#### 処理
1. ディレクトリ走査
2. Default / Profile X 抽出
3. 不要フォルダ除外

#### エラー処理
- 存在しない場合はスキップ
- アクセス拒否は無視

---

### 4.2 プロファイル情報取得

#### ファイル
```
Local State
```

#### 処理
- JSON解析
- name取得

#### フォールバック
- ディレクトリ名使用

---

### 4.3 アイコン取得（重要）

#### 優先順位（フォールバック）
1. Google Profile Picture.png
2. Google Profile.ico（上記PNGがない場合）
※いずれも見つからない場合はアイコンを表示しない（空白）。chrome.exeのアイコンは使用しない。

#### キャッシュ
- `%TEMP%\\ChromeProfileLauncher\\icons`
- 更新日時で再生成

---

### 4.4 ランチャ機能

#### 動作
- 起動済 → フォーカス
- 未起動 → 起動

#### 起動コマンド
```
chrome.exe --profile-directory="Profile X"
```

---

### 4.5 起動状態判定（改良）

#### 方法
- プロセス + ウィンドウハンドル管理
- 起動時にhwndを記録
- 終了時に自動削除

---

### 4.6 フォーカス制御

- Win32 API
- SetForegroundWindow

---

### 4.7 設定機能

- 並び替え（ドラッグハンドルを使用した直感的なドラッグ＆ドロップ）
- 表示／非表示（トグルスイッチによる切り替え）
- プロファイルフォルダ名を表示（プロファイル名の下に表示）
- プロファイルフォルダを開く機能（「OPEN」ボタン）
- ※プロファイル名およびアイコンの編集機能（独自の画像指定など）は提供しない

---

## 5. UI仕様

### 5.1 ランチャ画面

- タイトルバーなしのコンパクト表示（「Chrome Launcher」等のタイトル表示は廃止）
- 縦リスト表示（ダークテーマ採用のカード形式デザイン。カード背景色は視認性向上のため #222222 を採用）
- アイコン + 名前（左揃えレイアウト、文字色は白 #FFFFFF に固定）
- スムーズスクロール対応（ピクセル単位の滑らかなスクロール）
- 設定画面表示中はメイン画面全体をグレーアウト（半透明レイアウトを表示）
- 右下に設定ボタンを配置

---

### 5.2 設定画面

- 別ウィンドウ（ShowDialog）
- スムーズスクロール対応
- ドラッグハンドルによる並び替え（ドラッグ中のドロップ先アイテムを半透明化する視覚的フィードバック付き）
- トグルスイッチ（表示／非表示）
- プロファイルフォルダを開くボタン
- プロファイル名およびフォルダ名の表示（読み取り専用）
- 不要なラベル（「NAVIGATION MODULES」等）を排除したシンプルな構成

#### レイアウトイメージ
------------------------------------------------------
| Launcher Settings                                   |
------------------------------------------------------
| ☰ | 🖼 | 仕事        |      | [OPEN] |
|   |    | /Default    | ( ○) |        |
------------------------------------------------------
| ☰ | 🖼 | 個人        |      | [OPEN] |
|   |    | /Profile 1  | ( ○) |        |
------------------------------------------------------
| [キャンセル]                    [保存]               |
------------------------------------------------------
```

---

## 6. データ設計

### 設定ファイル

```json
{
  "profiles": [
    {
      "profileId": "Profile 1",
      "displayName": "仕事用",
      "visible": true,
      "order": 1
    }
  ]
}
```

### 保存場所

```
%AppData%\\ChromeProfileLauncher\\settings.json
```

---

## 7. 処理フロー

### 起動時
1. プロファイル取得
2. 設定読込
3. データマージ（Chrome側プロファイルをベースとし、設定があれば上書き、なければデフォルト追加）
4. アイコンキャッシュ確認
5. UI生成

### 操作時
1. 起動状態確認
2. フォーカス or 起動

---

## 8. エラーハンドリング

- ログ出力（debug.log）
- エラー時も処理継続

---

## 9. パフォーマンス

- 起動1秒以内（キャッシュ後）
- 初回は数秒

---

## 10. アーキテクチャ

```
UI
↓
ViewModel
↓
Service
```

---

## 11. タスクトレイ（将来）

- NotifyIcon
- 最小化で格納
- ダブルクリックで表示
- 右クリックでメニュー表示（ランチャ表示、設定、終了など）
- アプリ動作として `ShutdownMode = OnExplicitShutdown` を設定

---

## 12. 開発ステップ（詳細）

### Phase1 MVP
- プロファイル取得
- 起動機能
- 基本UI

### Phase2 機能追加
- 設定画面
- 並び替え
- 表示制御

### Phase3 UX改善
- アイコン
- キャッシュ
- 高速化

### Phase4 常駐化
- タスクトレイ

---

## 13. 制約

- Chrome仕様依存

---

## 14. 配布仕様

- 配布形式: 単一EXE
- ビルドコマンド例: `dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true`

---

## 15. 拡張・将来構想

- タスクトレイ常駐実装
- ショートカット等のホットキー連携
- 検索機能の追加
- 起動中プロファイルのハイライト表示

---

## 16. 総括

軽量・高速なプロファイルランチャを提供する。

